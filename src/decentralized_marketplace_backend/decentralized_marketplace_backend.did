type Item = record {
  id : nat64;
  name : text;
  description : text;
  principal_id : principal;
  amount : nat64;
};
type NewItem = record { name : text; description : text; amount : nat64 };
service : {
  list_item : (NewItem) -> (opt Item);
  return_items : () -> (vec Item) query;
}
